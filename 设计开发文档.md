# 设计开发文档

> 文档纪律：只呈现最终方案，不含变更记录。代码改动须同步更新本文件与 overview.md / README.md / setup.md。

## 1. 总体架构
客户端单页应用（`workspace.html`）运行于任意浏览器；静态资源由 **GitHub Pages** 托管；结构化数据 / 图片 / 博文以文件形式存于**同一仓库**，通过 **GitHub REST API** 读写。

```
[浏览器] ──打开 Pages 链接──▶ [GitHub Pages] 静态托管 workspace.html / images / posts
   │
   └──workerApi(path, opts) 带 x-app-key 闸门头──▶ [Cloudflare Worker weread-proxy] 统一后端网关
                                                    │  持有 GitHub PAT / 微信读书 Key（环境变量）
                                                    ├──GET/PUT /api/data    ─▶ GitHub Contents API 读写 data.json
                                                    ├──PUT/DELETE /api/image─▶ GitHub Contents API 读写 images/
                                                    └──POST /api/weread      ─▶ 微信读书网关 i.weread.qq.com/api/agent/gateway
```

## 2. 关键常量
```
WORKER_URL = 'https://weread-proxy.lucky888312.workers.dev'  // 公开，仅作中转，不含密钥
APP_KEY    = 'workbench-gate-2026'  // 轻量闸门，与 Worker 环境变量 APP_KEY 一致即可；非敏感凭证
// 以下常量仍保留在 workspace.html 的 CONFIG 中，仅供 Worker 缺失时的兜底说明，实际读写由 Worker 环境变量驱动：
CONFIG = { OWNER:'yizhigee', REPO:'work', BRANCH:'main', DATA_FILE:'data.json' }
// 凭证（GITHUB_PAT / WEREAD_KEY / GITHUB_OWNER / GITHUB_REPO / GITHUB_BRANCH / GITHUB_DATA_FILE / APP_KEY）
// 全部存于 Cloudflare Worker 环境变量，不在源码、不下发、不写 localStorage。
```

## 3. 存储层（全部经 Worker 网关）
- **data.json**：结构化数据。
  - 读：`workerApi('/api/data', {method:'GET'})` → Worker 代理 `GET .../contents/data.json?ref=main`，返回 `{ok, data(原始 JSON 串), sha, empty}`；404 按空数据处理。
  - 写：`workerApi('/api/data', {method:'PUT', body:{data, sha}})` → Worker 代理 `PUT`，返回 `{ok, sha}`；前端用返回 sha 做乐观并发。**last-write-wins**。
- **images/**：二进制图片。
  - 上传：`workerApi('/api/image', {method:'PUT', body:{path, content(base64)}})`（canvas 压缩后）。
  - 删除：`workerApi('/api/image', {method:'DELETE', body:{path}})`（Worker 内部取 sha 后删）。
  - 访问：`{PAGES_URL}images/{file}`（Pages 静态托管，不经 Worker）。
- **posts/**：博文，每篇 `posts/{yyyy}/{slug}.md` 或 `.json`，读写同上。

## 4. 数据模型
沿用现有中文字段 + `_id` 定位记录：
- `habits`: `[{_id, 名称, 类别, 目标, 图标, 创建时间}]`
- `checkRecords`: **压缩存储**——按年位串或「每习惯已打卡日期数组」（避免每天一条对象撑爆 1 MB）
- `todos`: `[{_id, 标题, 优先级, 状态, 截止, 标签, 备注}]`
- `focus`: `[{_id, 类型, 时长, 开始, 结束}]`
- `settings`: `{头像, 昵称, 签名, 模块顺序, 番茄默认}`
- 扩展模块：各加一个 key（如 `reading` / `fitness` / `anniversary` / `notes` / `stats`）
- `books`: 微信读书书架（`[{_id, title, author, cover, category, progress, readTime, noteCount, status, raw}]`）
- `readStats`: 微信读书统计报告对象（总时长 / 阅读天数 / TOP10 / 分类 / 历年趋势 / 进度分布 / 作者等）
- `notebooks`: 微信读书笔记（`[{_id, bookId, title, noteCount, notes, raw}]`）
- `readingPlans`: 手动阅读计划（`[{_id, title, due, status, note, created}]`）

`data.json` 初始空结构：
```json
{
  "habits": [], "records": [], "todos": [],
  "focus": [], "reading": [], "fitness": [], "anniversaries": [], "notes": [],
  "books": [], "readStats": null, "notebooks": [], "readingPlans": [],
  "settings": { "avatar":"", "nick":"", "sign":"", "order":["home","habits","todos","focus","reading","fitness","anniversaries","notes","stats","settings"], "focusDefault":25 }
}
```

## 5. API 层（统一封装）
- `loadFromRepo()` / `saveToRepo()`：`data.json` 读写（经 `/api/data`）。
- `uploadImageToRepo(file)` / `deleteImageFromRepo(name)`：图片（经 `/api/image`）。
- `wereadFetch(apiName, params)` / `syncWeread()`：微信读书同步（经 `/api/weread`）。
- 统一网关封装 `workerApi(path, opts)`：自动带 `x-app-key: APP_KEY` 闸门头，对返回 JSON 统一解析（`{ok, ...}` 约定）。
- 认证：GitHub PAT 与微信读书 Key 由 Worker 在环境变量中注入，**前端不持有任何 Bearer 凭证**，源码 / 浏览器 / localStorage 均不含 token。
- 微信读书同步：`wereadFetch(apiName, params)` → `POST /api/weread`（body 即 `{api_name, skill_version, ...}`）→ Worker 注入 `Authorization: Bearer <WEREAD_KEY>` 转发 `i.weread.qq.com/api/agent/gateway`，原样返回微信读书 JSON。
- 限流：GitHub API 5000 次 / 小时；微信读书网关按 Skill 策略，个人手动操作无感。

## 6. 同步与并发
last-write-wins，无合并。单人、同设备单标签，风险极低；多端同时改后存覆盖先存。

## 7. 容量策略
- **data.json**：打卡按年分片（如 `checkRecords-2026`）+ 位串压缩（每年 366 天用 366 个 `0/1`，30 习惯 ≈ 11 KB/年）；单文件 ≤ 1 MB。
- **图片**：canvas 缩至 ~1280px、JPEG 70–80%，100–300 KB/张。
- **博文**：独立文件，不占 `data.json` 额度。
- **逃生舱**：Git Data API（blob 100 MB），写需 blob→tree→commit 三步。

## 8. 安全
- **凭证统一托管在后端**：GitHub PAT（fine-grained，**仅授权本仓库** `Contents: Read and Write`）与微信读书 Key 全部存于 Cloudflare Worker 环境变量。前端 `workspace.html` 零 token、零 localStorage 密钥。即便公开仓库源码 / 页面链接外传，也拿不到任何可写凭证。
- **前端轻量闸门 APP_KEY**：`workspace.html` 内的 `APP_KEY` 常量（如 `workbench-gate-2026`）随每个请求以 `x-app-key` 头发送；Worker 端 `checkGate()` 校验一致才放行。APP_KEY 非有权限凭证，仅挡随机滥用（Worker 端未配置 APP_KEY 时 fail-open 放行）。
- **源码零明文 token**：GitHub PAT / 微信读书 Key 不写入 `workspace.html`、不写入 `weread-worker.js` 源码（仅运行期从环境变量读取）。
- 微信读书同步经 Worker 网关注入 Key 转发，Worker 不在服务端持久化额外状态。
- 原「设置 → 云端同步 Token / 微信读书同步」的 Key 输入 UI 已移除，改为「云端同步」卡片显示后端连接状态 + 微信读书「立即同步」按钮。
- 泄露处理：到 GitHub / 微信读书 Skill 吊销旧 token / Key，并在 Cloudflare Worker 后台更新对应环境变量即可（无需改前端源码、无需重新 push 前端；Worker 改完 Deploy 一次）。

## 9. 模块扩展规范
加模块三步走：
1. `workspace.html` 加 UI 区块 + 注册 `MODULES`；
2. `data.json` 加对应 key + 增删改函数（复用 `saveData`）；
3. 需图调 `uploadImage`，需长文存 `posts/`。
其余渲染 / 同步全自动复用现有机制。

## 10. 发布流程
push 到 `main` → GitHub Pages 自动部署（静态，无构建）。数据 / 图片 / 博文写入即对用户即时生效（Pages 静态缓存数分钟）。

## 11. 部署前置
见 `setup.md`。
