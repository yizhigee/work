# 设计开发文档

> 文档纪律：只呈现最终方案，不含变更记录。代码改动须同步更新本文件与 overview.md / README.md / setup.md。

## 1. 总体架构
客户端单页应用（`workspace.html`）运行于任意浏览器；静态资源由 **GitHub Pages** 托管；结构化数据 / 图片 / 博文以文件形式存于**同一仓库**，通过 **GitHub REST API** 读写。

```
[浏览器] ──打开 Pages 链接──▶ [GitHub Pages] 静态托管 workspace.html / images / posts
   │                                 ▲
   └──fetch 读 data.json(静态)──────┘
   └──Bearer PAT(本机localStorage)──▶ [GitHub REST API] 写 data.json / images / posts（Contents API）

[浏览器] ──Bearer 微信读书 Key(本机localStorage)──▶ [Cloudflare Worker] 透明转发 + CORS
   │                                                   │
   └───────────────────────────────────────────────────┘
   └── 微信读书网关 i.weread.qq.com/api/agent/gateway（不允许 GitHub Pages 直接跨域）
```

## 2. 关键常量（CONFIG）
```
CONFIG = {
  OWNER:       'yizhigee',
  REPO:        'work',
  BRANCH:      'main',
  DATA_FILE:   'data.json',
  GITHUB_TOKEN:''   // 占位兜底；正式 PAT 由用户首次打开时粘贴，存浏览器 localStorage（键 wb_github_pat），源码零明文
}
API_BASE = 'https://api.github.com/repos/{OWNER}/{REPO}/contents'
PAGES_URL = 'https://{OWNER}.github.io/{REPO}/'
// 注：images/ 与 posts/ 为规划中的图片/博客存储约定，上传 UI 待实现（见 §3）。
```

## 3. 存储层
- **data.json**：结构化数据。
  - 读：`GET {API_BASE}/data.json?ref={BRANCH}`（优先带本机 PAT 避免匿名限流；无 token 时降级为匿名读，仓库无该文件时返回 404，按空数据处理）。
  - 写：`PUT {API_BASE}/data.json`，body 含 base64 内容与 commit message；**last-write-wins**。
- **images/**：二进制图片。
  - 上传：`PUT {API_BASE}/images/{file}`（canvas 压缩后的 base64）。
  - 删除：`DELETE {API_BASE}/images/{file}`。
  - 访问：`{PAGES_URL}images/{file}`（Pages 静态托管）。
- **posts/**：博文，每篇 `posts/{yyyy}/{slug}.md` 或 `.json`，读写同上。

## 4. 数据模型
沿用现有中文字段 + `_id` 定位记录：
- `habits`: `[{_id, 名称, 类别, 目标, 图标, 创建时间}]`
- `checkRecords`: **压缩存储**——按年位串或「每习惯已打卡日期数组」（避免每天一条对象撑爆 1 MB）
- `todos`: `[{_id, 标题, 优先级, 状态, 截止, 标签, 备注}]`
- `focus`: `[{_id, 类型, 时长, 开始, 结束}]`
- `settings`: `{头像, 昵称, 签名, 模块顺序, 番茄默认}`
- 扩展模块：各加一个 key（如 `reading` / `fitness` / `anniversary` / `notes` / `stats`）
- `books`: 微信读书书架（`[{_id, title, author, cover, category, progress, readTime, noteCount, status, raw}]`）
- `readStats`: 微信读书统计报告对象（总时长 / 阅读天数 / TOP10 / 分类 / 历年趋势 / 进度分布 / 作者等）
- `notebooks`: 微信读书笔记（`[{_id, bookId, title, noteCount, notes, raw}]`）
- `readingPlans`: 手动阅读计划（`[{_id, title, due, status, note, created}]`）

`data.json` 初始空结构：
```json
{
  "habits": [], "records": [], "todos": [],
  "focus": [], "reading": [], "fitness": [], "anniversaries": [], "notes": [],
  "books": [], "readStats": null, "notebooks": [], "readingPlans": [],
  "settings": { "avatar":"", "nick":"", "sign":"", "order":["home","habits","todos","focus","reading","fitness","anniversaries","notes","stats","settings"], "focusDefault":25 }
}
```

## 5. API 层（统一封装）
- `loadData()` / `saveData()`：`data.json` 读写。
- `uploadImage(file)` / `deleteImage(name)`：图片。
- `loadPost(slug)` / `savePost(slug, content)` / `listPosts()`：博文。
- 认证头：`Authorization: Bearer <本机 localStorage 中的 PAT>`（运行时从 `getToken()` 读取，源码不含明文）。
- 微信读书同步：`wereadFetch(apiName, params)` → POST 到 Cloudflare Worker → Worker 透传 `Authorization: Bearer <wb_weread_key>` → 微信读书网关 `i.weread.qq.com/api/agent/gateway`。Worker 仅加 CORS 头、不存 Key、不改请求体。
- 限流：GitHub API 5000 次 / 小时；微信读书网关按 Skill 策略，个人手动操作无感。

## 6. 同步与并发
last-write-wins，无合并。单人、同设备单标签，风险极低；多端同时改后存覆盖先存。

## 7. 容量策略
- **data.json**：打卡按年分片（如 `checkRecords-2026`）+ 位串压缩（每年 366 天用 366 个 `0/1`，30 习惯 ≈ 11 KB/年）；单文件 ≤ 1 MB。
- **图片**：canvas 缩至 ~1280px、JPEG 70–80%，100–300 KB/张。
- **博文**：独立文件，不占 `data.json` 额度。
- **逃生舱**：Git Data API（blob 100 MB），写需 blob→tree→commit 三步。

## 8. 安全
- PAT 用 fine-grained，**仅授权工作台这一仓库**的 `Contents: Read and Write`，不授权其他仓库 → 泄露爆炸半径锁死在本仓库。
- **源码零明文 token**：PAT 不写入 `workspace.html`，改为存本机浏览器 localStorage（键 `wb_github_pat`）。公开仓库部署下，源码可读也拿不到 token → 链接外传无法读写数据；仅本机已粘贴 token 的浏览器可写。
- 微信读书 API Key 同样**仅存本机浏览器 localStorage**（键 `wb_weread_key`），不写入源码、不写入 `weread-worker.js`。Cloudflare Worker 只做透明转发，不在服务端持久化任何 Key。
- 首次打开无 PAT 会弹窗要求粘贴；「设置 → 云端同步 Token」可查看尾号、重新保存或清除。「设置 → 微信读书同步」可管理微信读书 Key / Worker 地址。
- 泄露处理：到 GitHub / 微信读书 Skill 吊销旧 token，本机清除对应 localStorage 项后重新粘贴新 token（无需改源码、无需重新 push）。

## 9. 模块扩展规范
加模块三步走：
1. `workspace.html` 加 UI 区块 + 注册 `MODULES`；
2. `data.json` 加对应 key + 增删改函数（复用 `saveData`）；
3. 需图调 `uploadImage`，需长文存 `posts/`。
其余渲染 / 同步全自动复用现有机制。

## 10. 发布流程
push 到 `main` → GitHub Pages 自动部署（静态，无构建）。数据 / 图片 / 博文写入即对用户即时生效（Pages 静态缓存数分钟）。

## 11. 部署前置
见 `setup.md`。
